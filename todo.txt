auto question - in excel
pagination
add model
change post to topic
topic -> dropdown
django:
    change password
    login required
    logout user
optimise:
    userservice: return name instead of returning all users then iterate (postlist)
    combine auto add and manual add
    manual score added from csv file
    student answers
optimise answer in student page using search algo
dependencies on readme.md